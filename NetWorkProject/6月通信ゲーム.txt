 ///レッツガチンコバトル！

・プレイヤーが出来る事
  プレイヤーは操作キャラクターの
  前方に弾を発射できる

・ギミック
  オブジェクトの壁　
  弾が壁に当たると跳ね返る()

・ルール
  ポイント制
  自身のキャラクターに弾が当たると対戦相手のポイントが加算される。

  1vs1の2D通信対戦ゲーム


・キャラクター操作
  マウスのカーソル位置に左クリックで弾を発射。
  WASDキーで上下左右移動。


////////////////////////////////////
・サーバー処理
各キャラクターの座標x,y
      //      のHP
      //      のポイント

弾丸スポーン処理()

各弾丸の座標x,y

弾丸の発射方向ベクトルx,y

当たり判定()

ダメージ()

弾が壁に跳ね返る処理()


・クライアント処理

キャラクターの移動ベクトルx,yをサーバへ渡す

弾丸の移動ベクトルx,yをサーバーへ渡す

サーバーから受け取った座標x,yを元に描画()

//基本的にサーバ側で機能処理をするほうがラグが少なくてすむ
ただし負荷がかかるためサーバーが重くなる。

1vs1なら問題ない。

//
追加したいもの
タイムリミット
